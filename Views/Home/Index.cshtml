@* 

 @model IEnumerable<Demo_Code_First.Models.ProductDto>;

<div class="row">
    @foreach (var product in Model)
    {
        await Html.RenderPartialAsync("_ProductCard", product); 
    }
</div> *@